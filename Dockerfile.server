# Dockerfile.server
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
COPY tsconfig*.json ./
COPY server/ ./server
COPY shared/ ./shared

RUN npm ci --include=dev

CMD ["node", "--loader", "tsx", "server/index.ts"]
