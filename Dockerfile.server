FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
COPY tsconfig*.json ./
COPY server/ ./server
COPY shared/ ./shared

# Устанавливаем зависимости, включая dev-зависимости
RUN npm ci --omit=dev=false

# Запуск через npx tsx (так надёжнее)
CMD ["npx", "tsx", "server/index.ts"]
