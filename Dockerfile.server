# Dockerfile.server

FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
COPY tsconfig*.json ./
COPY server/ ./server
COPY shared/ ./shared

RUN npm ci
RUN npx tsc -p tsconfig.server.json

CMD ["node", "dist/server/index.js"]
